---
name: huanyuan
description: 将已经完成的图片版 PPT/PPTX 还原为可编辑文字版，同时尽量保留原有视觉风格、版式、氛围和图片设计。适用于用户要求去掉图片中文字、重生成无字背景、再用 PPT 文本框加回相同文案的场景。
---

# Huanyuan

用于处理“图片版 PPT 已经做好，但文字不可编辑”的还原任务。目标不是重新设计一套新 PPT，而是在保留原设计风格的前提下，把页面上的正式文字从图片中剥离出来，重新做成 PPT 原生可编辑文本。

## 适用场景

使用本技能，当用户提出类似需求：

- “把这套图片版 PPT 做成可编辑版”
- “去掉图片上的文字，再用 PPT 加回同样的文字”
- “保留原来的设计风格，但文字要能编辑”
- “针对已经完成的图片版课件，还原成可改文字的 PPT”

如果用户只是要求重新美化、重做视觉、生成图片版课件，优先使用美化/课件生成类流程；只有当“图片中的文字需要转成 PPT 可编辑文字”是明确目标时，使用本技能。

## 工作原则

1. 先确认源文件  
   找到用户指定的原图片版 PPT/PPTX。不要默认改动之前生成的其他版本；如果同目录有多个近似文件，优先使用用户明确点名的那一个。

2. 保留原设计，不重做审美  
   还原任务的重点是“可编辑化”，不是推翻原方案。应尽量保留原来的底色、插画、构图、红线、纸张质感、人物关系、视觉节奏和章节气质。

3. 文字分层处理  
   页面上的正式标题、正文、标签、金句、页码、按钮文字等，应从背景图中去除，并重新用 PPT 文本框添加。最终用户应能在 PowerPoint 中直接选中和编辑这些文字。

4. 背景图重新生成  
   对每页原始图片进行“去文字、补背景”的图像处理。生成结果应是同尺寸、同构图、无正式可读文字的背景图。插画中的极小屏幕纹理或不可读伪文字可保留，除非用户要求彻底清空。

5. 不牺牲可读性  
   叠加回 PPT 的文字应匹配原页面的层级、位置、对齐、字号、字重、颜色和留白。特别注意标题与正文不能同级；主标题、副标题、章节标题、正文、注释应有明显层级。

6. 保留可用信息  
   尽量保留原 PPT 的页数、页面顺序、备注、链接、视频触发页、目录关系和讲师提示。若无法自动保留，应在交付时说明。

## 推荐流程

1. 解析源 PPT  
   导出每页高清图片，识别每页可见文字、文字层级、位置和页面用途。必要时读取备注页、已有文案文件或构建清单。

2. 生成无字背景  
   逐页以原页面图片为参考，使用图像编辑能力去除正式文字并补齐背景。要求输出与原幻灯片比例一致的 PNG。

3. 重建 PPT  
   每页先铺满对应的无字背景图，再按原位置添加 PPT 文本框。文字应使用常见可编辑字体，中文优先使用 Microsoft YaHei、思源黑体、等线或用户指定字体。

4. 匹配视觉层级  
   依据原页面重新设置字号、字重、颜色、透明度、行距、段距、文本框宽度和对齐方式。不要让所有文字看起来同级。

5. 校验交付  
   至少检查以下事项：
   - 页数与源 PPT 一致；
   - 每页都有无字背景图；
   - 正式文字是 PPT 原生文本，不是烙在图片里；
   - 关键链接、备注、视频页提示已保留；
   - 渲染预览中没有明显重影、错位、遮挡或字体层级错误；
   - 输出文件使用新文件名，不覆盖源文件。

## 交付格式

输出文件名建议包含“无字图可编辑文字版”或“还原可编辑版”，例如：

`原文件名-无字图可编辑文字版.pptx`

如同时生成了无字背景图，可放入独立目录，例如：

`pages-textless/slide-01.png`

最终回复用户时，简要说明：

- 已基于哪一个源 PPT 处理；
- 输出 PPT 路径；
- 是否完成页数、背景图、可编辑文字、备注和链接校验；
- 如有残留的插画微纹理或无法还原的信息，明确指出。

## 质量红线

- 不要把整页截图直接盖回去后声称“文字可编辑”。
- 不要生成一套风格完全不同的原生 PPT 来替代用户要还原的图片版。
- 不要忽略用户点名的源文件。
- 不要让标题、正文、注释全部使用同一字号和字重。
- 不要覆盖原始 PPT。
